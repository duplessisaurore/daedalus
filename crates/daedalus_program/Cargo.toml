[package]
name = "daedalus_program"
version = "1.0.0"
edition = "2024"
authors = ["The Daedalus Contributors"]

description = "A library that embeds daedalus programs into the rust build process as a static array"
readme = "README.md"

repository = "https://github.com/duplessisaurore/daedalus"
license = "AGPLv3"

keywords = ["languages", "scripting", "script"]

[dependencies]
lepton3 = { workspace = true }

[build-dependencies]
serde = { version = "1", features = ["derive"] }
toml = "1"
lepton3 = { workspace = true }


[profile.release]
# Try optimise harder for even better performance..
codegen-units = 1
lto = true
